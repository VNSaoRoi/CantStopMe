# Security

CantStopMe generates command-injection bypass strings for **authorized security education and labs only**.

Do not use against systems without explicit written permission. Misuse may violate law and policy.

Report issues via the GitHub issue tracker (avoid posting live exploit payloads in public issues).
